This module allows to track all changes made on sale orders during a cancel → draft → reconfirm cycle.
Every field change made while the order is in draft (on the order itself or its lines) is recorded using the tracked fields defined in tracking_manager (native Odoo tracking or custom tracking).
