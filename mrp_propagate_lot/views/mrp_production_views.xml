<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="mrp_production_form_view" model="ir.ui.view">
        <field name="model">mrp.production</field>
        <field name="inherit_id" ref="mrp.mrp_production_form_view" />
        <field name="arch" type="xml">
            <field name="lot_producing_id" position="before">
                <field name="auto_generate_lot" invisible="1"/>
            </field>
            <field name="lot_producing_id" position="attributes">
                <attribute name="attrs">{'invisible': [('product_tracking', 'in', ('none', False))], 'readonly': [('auto_generate_lot', '=', True)]}</attribute>
            </field>
        </field>
    </record>

</odoo>
