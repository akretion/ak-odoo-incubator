This module restricts the editability of the 'initial demand' and 'quantity done' field on stock moves.
Specifically, it makes the field non-editable when the move is in 'done' or 'cancel' state.
if the stock move is linked to a sale order, the 'initial demand' field is also non-editable.

