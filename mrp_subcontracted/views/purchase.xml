<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="purchase.purchase_rfq" model="ir.actions.act_window">
            <field name="domain">[('state','in',('draft','sent','cancel', 'confirmed', 'po_sent'))]</field>
        </record>

        <record id="purchase.purchase_form_action" model="ir.actions.act_window">
            <field name="domain">[('state','not in',('draft','sent', 'po_sent', 'confirmed'))]</field>
        </record>

    <record id="purchase_order_form" model="ir.ui.view">
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.purchase_order_form"></field>
        <field name="arch" type="xml">
            <field name="picking_ids" position="after">
                <button type="object"
                    name="action_view_mo"
                    class="oe_stat_button"
                    icon="fa-industry" attrs="{'invisible':[('manufacture_ids','=',[])]}">
                    <field name="mo_count" widget="statinfo" string="Manufacture" help="Manufacture Orders"/>
                    <field name="manufacture_ids" invisible="1"/>
                </button>
            </field>
            <field name="picking_type_id" position="attributes">
                <attribute name="domain">[('code','in',('incoming', 'mrp_operation'))]</attribute>
            </field>
        </field>
    </record>
</odoo>
