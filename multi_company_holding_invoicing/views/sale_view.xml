<?xml version="1.0" encoding="utf-8" ?>
<odoo>

<record id="view_order_form" model="ir.ui.view">
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_order_form" />
    <field name="arch" type="xml">
        <xpath
                expr="//group[@name='sale_info']/field[@name='invoice_status']"
                position="after"
            >
           <field name="holding_invoice_state" />
           <field name="holding_invoice_id" />
        </xpath>
    </field>
</record>

<record id="view_sales_order_filter" model="ir.ui.view">
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_sales_order_filter" />
    <field name="arch" type="xml">
        <filter string="Salesperson" position="after">
            <filter name="team_id" string="Section" context="{'group_by':'team_id'}" />
            <filter
                    name="holding_invoice_id"
                    string="Holding Invoice"
                    context="{'group_by':'holding_invoice_id'}"
                />
        </filter>
        <field name="product_id" position="after">
           <field
                    name="invoice_ids"
                    domain="[
                      ('type', '=', 'out_invoice'),
                      ('sale_ids', '!=', False),
                      ]"
                />
           <field
                    name="holding_invoice_id"
                    domain="[
                      ('type', '=', 'out_invoice'),
                      ('holding_sale_ids', '!=', False),
                      ]"
                />
        </field>
    </field>
</record>

</odoo>
